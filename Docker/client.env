# ============================================================================
# UDP-Speeder-Manager - Client Environment Variables
# ============================================================================
# Project: UDP-Speeder-Manager
# Author: iHub-2020
# Version: v2.0.0
# Date: 2026-01-17
# Description: Client mode configuration template
# Usage: Copy to .env and modify as needed
# ============================================================================

# ========== Basic Configuration ==========
MODE=client
LOCAL_ADDR=0.0.0.0
LOCAL_PORT=3333

# Server Connection (REQUIRED)
REMOTE_ADDR=44.55.66.77       # Replace with your server IP
REMOTE_PORT=29900

# FEC Parameters (must match server)
FEC_PARAMS=20:10

# Authentication Password (must match server)
PASSWORD=your_secure_password_here

# Work Mode (0=bandwidth-saving, 1=lower-latency)
WORK_MODE=0

# FEC Timeout (milliseconds)
TIMEOUT=8

# ========== Advanced Configuration (Optional) ==========
# Uncomment to override defaults

# MTU=1250                    # MTU size (default: 1250, don't change unless necessary)
# QUEUE_LEN=200               # FEC queue length (mode 0 only, default: 200)
# INTERVAL=0                  # Scatter interval in ms to protect burst loss (default: 0)
# JITTER=0                    # Simulated jitter in ms (default: 0)
# REPORT=10                   # Send/recv report interval in seconds (0=disabled)
# DISABLE_OBSCURE=0           # Disable packet obfuscation (0=enabled, 1=disabled)
# EXTRA_ARGS=                 # Additional command-line arguments

# ========== System Configuration ==========
PUID=1000
PGID=1000
TZ=Asia/Shanghai

# Persistent Directories
CONFIG_DIR=./data/config
LOG_DIR=./data/logs
